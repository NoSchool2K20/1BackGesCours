language: node_js

script:
     - echo Hello pour mon premier build travis
     - npm install 
     - npm run build
     - echo "$PREPROD_PEM" > preprod.pem
     - cat preprod.pem
     - chmod 600 preprod.pem
     - echo | ssh -o StrictHostKeyChecking=no -i preprod.pem ec2-user@ec2-18-220-58-155.us-east-2.compute.amazonaws.com ./start.sh

after_success:
    - echo Fin